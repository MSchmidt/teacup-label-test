# -*- coding: utf-8 -*-
$:.unshift("/Library/RubyMotion/lib")
require 'motion/project/template/ios'

begin
  require 'bundler'
  Bundler.require
rescue LoadError
end

require 'teacup'

Motion::Project::App.setup do |app|
  # Use `rake config' to see complete project settings.
  app.xcode_dir = '/Applications/Xcode51-Beta5.app/Contents/Developer'
  app.sdk_version = '7.1'
  app.deployment_target = '7.0'
  app.name = 'trytry'
end
